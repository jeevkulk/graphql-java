type Person {
    firstName: String!
    lastName: String!
    age: Int!
    city: String!
}

type Query {
    allPeople: [Person]
}

type Mutation {
    createPerson(firstName: String!, lastName: String!, age: Int!, city: String!): Person
}

schema {
    query: Query
    mutation: Mutation
}